<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List App</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://unpkg.com/htmx.org@1.6.1"></script>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-4">
        <h1 class="text-3xl font-bold mb-4 bg-blue-300 text-center rounded">Todo List</h1>
        <form id="todo-form" class="mb-4" hx-post="/add" hx-target="#todo-container" hx-swap="innerHTML">
            <div class="flex mb-4">
                <input type="text" id="todo-input" name="task" class="flex-1 p-2 border border-gray-300 rounded" placeholder="Add a new task" required>
                <input type="date" id="todo-deadline" name="deadline" class="ml-4 p-2 border border-gray-300 rounded">
                <button type="submit" class="p-2 bg-blue-500 text-white rounded ml-4">Add</button>
            </div>
        </form>
        <div id="todo-container">
            <%- include('partials/todo_list', { todos, notCompletedCount }); %>
        </div>
    </div>
</body>
</html>